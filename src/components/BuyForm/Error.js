const Error = () => {
    return (
        <div className="container">
            <h1>Error</h1>
            <a data-atd="back-btn" href="#" onClick={props.reset}>back to buy</a>
        </div>
    )
}